# LOGO RULES (never violate):

- All `<img>` and `<Image>` tags for logos must have `className="bg-transparent"`
- Parent containers wrapping logos must never have `bg-*`, `background`, or `backgroundColor` set
- Logo images are always PNG with alpha channel — never add a background
- Use `mix-blend-mode: multiply` on dark backgrounds if needed for blending
